<div id="signin">
	<h2>Sign In</h2>

	<%= form_tag(session_path, remote: true) do %>
		<%= label_tag(:email) %>
		<%= email_field_tag(:email) %>

		<%= label_tag(:password) %>
		<%= password_field_tag(:password) %>

		<%= submit_tag("Sign In") %>
	<% end %>
</div>
